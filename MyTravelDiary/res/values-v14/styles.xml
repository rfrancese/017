<resources>

    <!--
        Base application theme for API 14+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 14+ devices.
    -->
    
        <!-- stile action bar -->
     <style name="ActionBarTheme" parent="@style/Theme.AppCompat">
        <item name="android:actionBarStyle">@style/actionBar</item>
        <item name="actionBarStyle">@style/actionBar</item>
        <item name="android:actionBarItemBackground">@color/cyan</item>
    </style>

   <style name="actionBar" parent="@style/Widget.AppCompat.ActionBar.Solid">
        <item name="android:background">@color/cyan</item>
        <item name="background">@color/cyan</item>
    </style>
    
    <color name="cyan">#33B5E5</color>

</resources>
